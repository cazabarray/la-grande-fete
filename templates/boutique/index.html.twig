{% extends 'base.html.twig' %}

{% block title %}Boutique | {{ parent() }}{% endblock %}

{% block body %}
    <div class="articles">
        {% for article in articles %}
            <div class="article">
                <h2>{{ article.nom }}</h2>
                <p>{{ article.description }}</p>
                <p><strong>{{ article.prix }} €</strong></p>
                <a href="{{ path('app_panier_ajout', { id: article.id }) }}">Ajouter l'article au panier</a>
            </div>
        {% endfor %}
    </div>
{% endblock %}
